apiVersion: v1
kind: Service
metadata:
  name: bookly-backend-service
  namespace: default
  labels:
    app: bookly-backend
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: bookly-backend
